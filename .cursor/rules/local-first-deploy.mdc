---
description: 本地先改、浏览器验收；禁止擅自 git push / 部署 GitHub Pages，推送时机由用户明示。
alwaysApply: true
---

# 本地优先 · 部署由用户控制

## 日常改原型

1. **只在本地改**：改完后启动/保持本地服务（通常 `pnpm start` → http://localhost:8000），让用户在浏览器验收。
2. **禁止自动推送**：改完一个点、修完一个 bug、或一轮小改结束后，**不要** `git push`，也**不要**主动触发 GitHub Pages 部署。
3. **何时推送**：仅当用户明确说例如「推到 GitHub」「部署一波」「可以上线了」「push 部署」时，才 `commit`（若用户要求）+ `push`。
4. 用户未要求提交时，也不要擅自 `git commit`（与用户全局规则一致）。

## 典型节奏

- **当天**：本地连续改 → 本地预览验收 → 不碰远程
- **下班 / 节点**：用户说部署 → 再 push `main`（Actions 部署 Pages）

## 反例

- ❌ 每修一个 logo / 样式就 push 一次
- ❌ 改完不等用户确认就部署
- ✅ 本地跑起来给用户看；用户说「部署」再推
