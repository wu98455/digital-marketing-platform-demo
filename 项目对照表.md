# 项目对照表

> 改仓库名、`PUBLIC_PATH`、Demo 链接、页面标题时：**以本表为准**，再同步改 `web/` 与 `.github/`。

| 项 | 值 |
|----|-----|
| 中文产品名 / 系统标题 | 数字营销平台 |
| 本地文件夹（slug） | `digital-marketing` |
| 本地路径 | `D:\dev\projects\digital-marketing` |
| 前端工程目录 | **`web/`**（唯一；安装 / 启动 / 构建都在此） |
| GitHub 仓库名 | `digital-marketing-platform-demo` |
| GitHub 远程 | `https://github.com/wu98455/digital-marketing-platform-demo.git` |
| Demo | https://wu98455.github.io/digital-marketing-platform-demo/ |
| `PUBLIC_PATH` | `/digital-marketing-platform-demo/` |
| 常用演示账号 | `admin` / `123456`（另有 `demo` 等，见 `web/README.md`） |

> 交付 / Git 主体：`web/`。根目录 `doc/` 等为本地工作产物（不进 Git）。日常说明见 [README.md](./README.md)。

## Git 跟踪范围

| 提交 | 不提交 |
|------|--------|
| `web/`、`.github/`、根 `README.md`、`项目对照表.md`、`.gitignore` | `doc/`、分析资料、`.cursor/`、`node_modules` / `dist` / `.umi*` 等 |

## 检查清单

- [ ] 换机后能按根 README 在 `web/` 装依赖并启动  
- [ ] `PUBLIC_PATH` 与 Pages 子路径一致  
- [ ] 仅在明确要求「部署」时 push `main`
